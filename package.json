{
    "name": "handbrake-auto",
    "version": "1.0.0",
    "description": "Convert videos from a input folder to a output folder automatically",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc --build tsconfig.json",
        "deploy": "nexe --ico icon.ico --build -t windows -n handbrake-auto build/index.js --resource \"build/**/*\""
    },
    "author": "Jarrad Banks",
    "license": "ISC",
    "dependencies": {
        "handbrake-js": "^6.0.0",
        "nexe": "^4.0.0-beta.19",
        "single-line-log": "^1.1.2"
    }
}
